import { Event } from "@types";

type Props = {
    events: Event[];
};

const EventOverview: React.FC<Prop> = ({events}: Props) => {
    return (
        <>
        </>
    )
};

export default EventOverview;