export type RegisterData = {
  firstName: string;
  lastName: string;
  username: string;
  email: string;
  password: string;
  // // role: string;
};

export type LoginData = {
  email: string;
  password: string;
};
