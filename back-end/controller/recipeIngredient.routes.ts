/**
 * @swagger
 * components:
 *   schemas:
 *     RecipeIngredient:
 *       type: object
 *       properties:
 *         recipeingredientId:
 *           type: number
 *           format: int64
 *         recipeId:
 *           type: number
 *           format: int64
 *         ingredientId:
 *           type: number
 *           format: int64
 *         unit:
 *           type: string
 *         quantity:
 *           type: number
 *           format: int64
 */